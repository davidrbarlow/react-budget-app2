import React from 'react';

const SideBar = () => (
    <div>
      <img id={'transactionsButton'} src={'/images/transactions.svg'} 
      alt={'Transactions'} width={'64'} height={'64'}></img>
      <img id={'projectionButton'} src={'images/projection.svg'} 
      alt={'Projections'}
      width ={'64'} height={'64'}></img>
    </div>
);

export default SideBar;
